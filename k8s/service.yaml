apiVersion: v1
kind: Service
metadata:
  labels:
    module: portfolio
  name: portfolio
  namespace: larsvanherk
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  - name: healthz
    port: 5001
    protocol: TCP
    targetPort: 5001
  selector:
    module: portfolio
  sessionAffinity: None