apiVersion: v1
kind: Service
metadata:
  labels:
    module: portfolio
  name: portfolio
  namespace: larsvanherk
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    module: portfolio
  sessionAffinity: None
  type: NodePort