apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: landing-page
  namespace: larsvanherk
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    rollingUpdate:
      maxSurge: 5
      maxUnavailable: 0
  template:
    metadata:
      labels:
        module: portfolio
        app: ___GITLAB_CI_LABEL___
    spec:
      imagePullSecrets:
        - name: gitlab
      containers:
        - name: app
          image: registry.gitlab.com/work-portfolio/portfolio:0.1.6
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 3